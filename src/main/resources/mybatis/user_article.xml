<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userArticle">
	<resultMap id="baseResult" type="ArticleDO">
		<result property="id" column="id" />
		<result property="userId" column="user_id" />
		<result property="title" column="title" />
		<result property="description" column="description" />
		<result property="picUrl" column="picUrl" />
		<result property="url" column="url" />
		<result property="plugin" column="plugin" />
		<result property="detail" column="detail" />
		<result property="status" column="status" />
		<result property="gmtCreate" column="gmt_create" />
		<result property="gmtModified" column="gmt_modified" />
	</resultMap>
	
	<select id="getWXNewsList" resultMap="baseResult" parameterType="java.util.Map">
		select * from user_web_article where user_id=#{userId} and status=#{status} and namespace="wxnews";
	</select>
	
	<select id="getNewsList" resultMap="baseResult" parameterType="java.util.Map">
		<![CDATA[
		select * from user_web_article where user_id=#{userId} and status=#{status} and namespace="news" order by gmt_create desc;
		]]>
	</select>
	
	<select id="getNewsForwardList" resultMap="baseResult" parameterType="java.util.Map">
		<![CDATA[
		select * from user_web_article where user_id=#{userId} and status=#{status} and gmt_create < #{cursor}  and namespace="news" order by gmt_create desc limit #{size};
		]]>
	</select>
	
	<select id="getNewsBackList" resultMap="baseResult" parameterType="java.util.Map">
		<![CDATA[
		select * from user_web_article where user_id=#{userId} and status=#{status} and gmt_create > #{cursor}  and namespace="news" order by gmt_create limit #{size};
		]]>
	</select>
	
	<select id="getBannerList" resultMap="baseResult" parameterType="java.util.Map">
		select * from user_web_article where user_id=#{userId} and status=#{status} and namespace="banner";
	</select>
	
	
	<select id="getArticleById" resultMap="baseResult" parameterType="java.util.Map">
		select * from user_web_article where id=#{id} and status=#{status};
	</select>
	
</mapper>  